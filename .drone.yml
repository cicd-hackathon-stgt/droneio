workspace:
  base: /go
  path: src/github.com/cicd-hackathon-stgt/droneio/

pipeline:
  go:
    image: golang
    commands:
      - go build -v
      - go test -v -race
      - ./droneio
  system:
    image: golang
    commands:
      - ls -l
      - lsb_release
