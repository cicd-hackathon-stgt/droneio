workspace:
  base: /go
  path: src/github.com/cicd-hackathon-stgt/droneio

pipeline:
  rungo:
    image: golang:latest
    commands:
      - go build . 
      - docker build -t testDockerBuild .      
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
